name: 'hub_monetization_dbt'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ['models']
target-path: 'target'
clean-targets: ['target']

models:
  hub_monetization_dbt:
    +materialized: view
